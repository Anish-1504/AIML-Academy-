/* ============================================================
   MAIN.JS — App entry point
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  // Short delay so matrix canvas renders first
  setTimeout(runBoot, 200);
});
